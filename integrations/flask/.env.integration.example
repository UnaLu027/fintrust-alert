# Shared Flask project -> FinTrust FastAPI service
# Local Codespaces / development example:
FINTRUST_API_BASE_URL=http://127.0.0.1:8000

# Only needed when the shared Flask admin route is allowed to trigger refresh.
# Do not expose this value in browser JavaScript.
FINTRUST_INGESTION_TOKEN=
